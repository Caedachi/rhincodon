export const TYPES = {
    config: {
        clientId: Symbol(),
        guildId: Symbol()
    },
    constants: {
        commands: Symbol(),
        map: Symbol()
    },
    secrets: {
        discordToken: Symbol()
    }
};
